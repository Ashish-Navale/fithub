// placeholder for client-side Stripe usage
export const startStripeCheckout = async (plan) => {
  // In real app call backend to create checkout session
  alert('Stripe checkout placeholder for plan: ' + plan.title);
};
